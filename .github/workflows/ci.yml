# CI: typecheck / lint / vitest（golden・ドリフトチェック含む）。デプロイは含めない（apify pushは手動）。
name: ci

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm test
      # 追補v1.1 R2-4: bundle生成の成功までがCIの責務（apify pushは手動）
      - run: pnpm --filter @jp-opendata/actor-edinet-filings build
      - run: pnpm --filter @jp-opendata/actor-real-estate-prices build
