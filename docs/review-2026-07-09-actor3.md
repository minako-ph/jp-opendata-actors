# Actor#3 レビュー結果と修正指示（2026-07-09）— jp-opendata-actors

保存先: `docs/review-2026-07-09-actor3.md` ／ 実施者: Claude Code
検証: dc5467fを第三者環境でclone→install→typecheck→**121テスト**→両Actorのbundle生成→#1バンドルのESM起動、すべて確認済み。

**判定: 巻き戻し不要。実装は正典（要件書FR-3／追補R2-3・R2-5・R2-6／引継書／マーケ§5.1）に準拠しており、品質は#1と同水準。** 指示書の出所が不明でも、内容はリポジトリのdocsから正しく導出されていた（＝docs体系が設計どおり機能した）。必須修正は以下の1件のみ。

---

## FIX-1【必須】aggregate.ts のリテラルNULバイト除去

`src/aggregate.ts` の複合キー区切りに**生のNUL文字（\x00・位置1926）**が埋め込まれており、gitがファイル全体をバイナリ判定している（diff・レビュー・部分編集が今後すべて不能）。区切り文字としての意図は理解できるが、副作用が大きすぎる。

- 修正: リテラルNULを文字列エスケープ `'\u0000'` に置換する（挙動は完全に同一）。例:
  `const key = \`${item.municipality_code}\u0000${item.period}\`;` → テンプレートリテラル内なら `` `${item.municipality_code}\u0000${item.period}` `` と書けばソースはASCIIのまま。
- 完了確認: ①`git diff --numstat` で当該ファイルが数値行数（バイナリの`-`でない）になる ②`file src/aggregate.ts`がtext判定 ③テスト121本グリーン維持。

## FIX-2【記録のみ・コード変更なし】無料枠50件の出自を1行補記

実装の50件/実行は妥当だが、追補R2-3の仮置きは**#3=100件**だった。decisions.mdの該当行（2026-07-09 #3設計確定）に「R2-3の仮置き100件から50件へ変更（いずれも仮置き。最終確定は公開時の事業主判断＝launch文書タスク）」の一句を補い、逸脱の来歴を明示する。

## 是認事項（レビュー済み・変更しないこと）

未決#4の一次資料＋実証による解消（01/02/省略の意味論、549=278+271）／ja/en二重取得のindex結合とサニティ検証（私の引継書§4.3「応答が日英両フィールドを含む」という記述は**誤りで、実装側が正**——引継書の当該記述は本レビューで訂正済みとみなす）／キーのヘッダ限定（F-1維持）とfixture無混入／集計の非課金・課金上限打ち切り時の集計抑止／createRequireバナーによる#1含むESMクラッシュ修正（実起動で確認）／監視のgov-clients/monitoring共通化／`real-estate-tx`→`real-estate-prices`のスラッグ準拠改名／README（§5.3必須節・REINS由来の正直注記・出典逐語）／decisions.mdへの設計判断記録。

## 完了条件

FIX-1・FIX-2反映で `pnpm typecheck && pnpm test` グリーン、`git diff`が全ファイルでテキスト扱い、decisions.mdに本レビュー対応の1行、push。
